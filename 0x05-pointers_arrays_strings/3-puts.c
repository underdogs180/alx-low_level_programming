
#include "holberton.h"
#include <stdio.h>

/**
 * _puts - a function used to print out a string on stdout.
 * @str: hold the value of a string.
 *
 * Return: Always 0.
 */
void _puts(char *str)
{
  puts(str);
}
