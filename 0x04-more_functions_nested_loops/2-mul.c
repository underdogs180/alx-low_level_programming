#include "holberton.h"
/**
 * mul - a function used to multiply two integers.
 *
 * @a: stores the variable of the datatype to be multiply.
 * @b: stores the variable of the datatype to be multiply.
 * Return: no return
 */
int mul(int a, int b)
{
  int x;
  x = a * b;
  return (x);
}
